module.exports = function (filepath){
	return filepath.slice(-5) === '.html' ? 'text/html'
		: filepath.slice(-4) === '.css' ? 'text/css'
		: filepath.slice(-3) === '.js' ? 'application/javascript'
		: filepath.slice(-4) === '.png' ? 'image/png'
		: 'text/html'
}